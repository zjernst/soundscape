const React = require('react');

const Footer = React.createClass({
  render() {
    let content
    return(
      <div className="footer">
        {content}
      </div>
    )
  }
});

module.exports = Footer;
